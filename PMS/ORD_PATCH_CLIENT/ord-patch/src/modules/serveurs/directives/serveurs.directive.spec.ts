import { ServeursDirective } from './serveurs.directive';

describe('ServeursDirective', () => {
  it('should create an instance', () => {
    const directive = new ServeursDirective();
    expect(directive).toBeTruthy();
  });
});
