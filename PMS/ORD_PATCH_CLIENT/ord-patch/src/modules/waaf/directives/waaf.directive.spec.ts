import { WaafDirective } from './waaf.directive';

describe('ServeursDirective', () => {
  it('should create an instance', () => {
    const directive = new WaafDirective();
    expect(directive).toBeTruthy();
  });
});
