import { CheckTimeLineDirective } from './check-time-line.directive';

describe('CheckTimeLineDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckTimeLineDirective();
    expect(directive).toBeTruthy();
  });
});
