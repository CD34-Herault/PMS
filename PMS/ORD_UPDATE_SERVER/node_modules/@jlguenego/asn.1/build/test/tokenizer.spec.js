"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const Rule_1 = require("../src/tokenizer/Rule");
const Tokenizer_1 = require("../src/tokenizer/Tokenizer");
describe('Tokenizer Unit Test', () => {
    it('should tokenize', () => {
        const input = `
    a aa aaa "aaa aaa"
aaaa
      `;
        const tokenizer = new Tokenizer_1.Tokenizer();
        const ruleset = [
            new Rule_1.Rule({
                name: 'comment',
                regexp: /--/,
                action: (ctx) => {
                    console.log('ctx: ', ctx);
                    // ctx.emit("blank")
                },
            }),
        ];
        tokenizer.addRules(ruleset);
        const tokens = tokenizer.tokenize(input);
        assert_1.default.deepStrictEqual(tokens, [{}]);
    });
});
//# sourceMappingURL=tokenizer.spec.js.map