"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Tokenizer = void 0;
/**
 * A tokenizer just split the input into many strings.
 * It does that by going through the input and raising events.
 *
 * @export
 * @class Tokenizer
 */
class Tokenizer {
    constructor() {
        this.ruleset = [];
    }
    addRule(rule) {
        this.ruleset.push(rule);
    }
    addRules(ruleset) {
        this.ruleset = this.ruleset.concat(ruleset);
    }
    tokenize(input) {
        console.log('input: ', input);
        return [];
    }
}
exports.Tokenizer = Tokenizer;
//# sourceMappingURL=Tokenizer.js.map