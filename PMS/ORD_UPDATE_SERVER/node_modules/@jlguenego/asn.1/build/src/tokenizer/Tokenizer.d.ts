import { Rule } from './Rule';
import { Token } from './Token';
/**
 * A tokenizer just split the input into many strings.
 * It does that by going through the input and raising events.
 *
 * @export
 * @class Tokenizer
 */
export declare class Tokenizer {
    ruleset: Rule[];
    constructor();
    addRule(rule: Rule): void;
    addRules(ruleset: Rule[]): void;
    tokenize(input: string): Token[];
}
