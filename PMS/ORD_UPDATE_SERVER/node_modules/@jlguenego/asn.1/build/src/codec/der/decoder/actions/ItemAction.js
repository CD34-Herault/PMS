"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ItemAction = void 0;
const Action_1 = require("../../../../Action");
const ActionType_1 = require("../../../../actions/ActionType");
const BOOLEAN = 0x01;
const INTEGER = 0x02;
const IA5STRING = 0x16;
const OBJECT_IDENTIFIER = 0x06;
class ItemAction extends Action_1.Action {
    constructor() {
        super(...arguments);
        this.type = ActionType_1.ActionType.ITEM;
    }
    transform(state) {
        const context = state.context;
        const type = state.dataview.getUint8(state.index);
        state.index++;
        context.index++;
        if (type === BOOLEAN) {
            state.nextAction = ActionType_1.ActionType.TYPE_BOOLEAN;
            return;
        }
        if (type === INTEGER) {
            state.nextAction = ActionType_1.ActionType.TYPE_INTEGER;
            return;
        }
        if (type === IA5STRING) {
            state.nextAction = ActionType_1.ActionType.TYPE_IA5STRING;
            return;
        }
        if (type === OBJECT_IDENTIFIER) {
            // state.nextAction = ActionType.ITEM_OBJECT_IDENTIFIER;
            throw new Error(`OBJECT_IDENTIFIER: ${type.toString(16)}`);
        }
        throw new Error(`type not found (or implemented): ${type.toString(16)}`);
    }
}
exports.ItemAction = ItemAction;
//# sourceMappingURL=ItemAction.js.map